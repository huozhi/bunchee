const add = (a, b)=>a + b;

const sum = add(1, 2);
const obj = {
    a: 1,
    b: 2
};
const clone = {
    ...obj
};
async function asyncFunc() {
    await new Promise((r)=>r(1));
}

export { asyncFunc, clone, sum as default };
